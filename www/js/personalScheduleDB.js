// Generated by CoffeeScript 1.4.0
var PersonalScheduleDB,
  __indexOf = [].indexOf || function(item) { for (var i = 0, l = this.length; i < l; i++) { if (i in this && this[i] === item) return i; } return -1; };

PersonalScheduleDB = (function() {

  PersonalScheduleDB.data;

  function PersonalScheduleDB() {
    this.data = JSON.parse(userconfig.getItem('personalSchedule', '[]'));
  }

  PersonalScheduleDB.prototype.getData = function() {
    return this.data;
  };

  PersonalScheduleDB.prototype.save = function() {
    return userconfig.setItem('personalSchedule', JSON.stringify(this.data));
  };

  PersonalScheduleDB.prototype.push = function(eventID) {
    eventID = parseInt(eventID);
    if (__indexOf.call(this.data, eventID) < 0) {
      this.data.push(eventID);
    }
    return this.save();
  };

  PersonalScheduleDB.prototype.remove = function(eventID) {
    var eventIDIndex;
    eventIDIndex = $.inArray(parseInt(eventID), this.data);
    if (eventIDIndex >= 0) {
      this.data.splice(eventIDIndex, 1);
    }
    return this.save();
  };

  PersonalScheduleDB.prototype.contains = function(eventID) {
    var _ref;
    return _ref = parseInt(eventID), __indexOf.call(this.data, _ref) >= 0;
  };

  return PersonalScheduleDB;

})();
